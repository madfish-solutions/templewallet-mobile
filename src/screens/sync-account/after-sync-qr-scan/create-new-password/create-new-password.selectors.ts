export enum CreateNewPasswordSyncAccountSelectors {
  PasswordInput = 'Password Input on Create a new Password page when Sync Account',
  RepeatPasswordInput = 'Repeat Password Input on Create a new Password page when Sync Account',
  SyncButton = 'Sync button on Create a new Password page when Sync Account'
}
