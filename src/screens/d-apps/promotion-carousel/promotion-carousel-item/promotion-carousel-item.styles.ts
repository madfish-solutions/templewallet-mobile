import { createUseStyles } from '../../../../styles/create-use-styles';

export const usePromotionCarouselItemStyles = createUseStyles(({ colors }) => ({
  container: {
    backgroundColor: colors.pageBG
  }
}));
