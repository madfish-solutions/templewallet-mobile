export enum SwapFormSelectors {
  fromAssetAmountInput = 'Swap (Form)/From Asset Amount Input',
  swapAssetsButton = 'Swap (Form)/Swap Assets Button',
  toAssetAmountInput = 'Swap (Form)/To Asset Amount Input',
  swapButton = 'Swap (Form)/Swap Button'
}
