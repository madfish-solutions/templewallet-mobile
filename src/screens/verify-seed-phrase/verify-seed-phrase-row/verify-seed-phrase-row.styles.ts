import { createUseStyles } from '../../../styles/create-use-styles';

export const useVerifySeedPhraseRowStyles = createUseStyles(() => ({
  container: {
    flexDirection: 'row',
    justifyContent: 'space-between'
  }
}));
