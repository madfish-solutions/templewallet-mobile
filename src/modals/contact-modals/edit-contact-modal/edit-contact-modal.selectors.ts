export enum EditContactModalSelectors {
  addressInput = 'Edit Contact (Modal)/Address Input',
  nameInput = 'Edit Contact (Modal)/Name Input',
  closeButton = 'Edit Contact (Modal)/Close Button',
  saveButton = 'Edit Contact (Modal)/Save Button'
}
