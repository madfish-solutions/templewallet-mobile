export enum AddContactModalSelectors {
  addressInput = 'Add Contact (Modal)/Address Input',
  nameInput = 'Add Contact (Modal)/Name Input',
  closeButton = 'Add Contact (Modal)/Close Button',
  saveButton = 'Add Contact (Modal)/Save Button'
}
