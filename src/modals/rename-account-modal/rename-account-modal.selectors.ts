export enum RenameAccountModalSelectors {
  nameInput = 'Rename Account (Modal)/Name Input',
  closeButton = 'Rename Account (Modal)/Close Button',
  saveButton = 'Rename Account (Modal)/Save Button'
}
