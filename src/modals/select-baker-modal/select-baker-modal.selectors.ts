export enum SelectBakerModalSelectors {
  searchBakerInput = 'Select Baker (Modal)/Search Baker Input',
  sortByDropDownButton = 'Select Baker (Modal)/Sort By Dropdown Button',
  closeButton = 'Select Baker (Modal)/Close Button',
  nextButton = 'Select Baker (Modal)/Next Button',
  bakerItem = 'Select Baker (Modal)/Baker Item'
}
