import { createUseStyles } from 'src/styles/create-use-styles';

export const useRebalanceModalStyles = createUseStyles(() => ({
  swapButtonContainer: {
    alignItems: 'center',
    justifyContent: 'center'
  }
}));
