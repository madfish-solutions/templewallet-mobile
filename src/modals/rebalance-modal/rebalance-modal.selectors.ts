export enum RebalanceModalSelectors {
  sourceInput = 'Rebalance (Modal)/Source Input',
  destInput = 'Rebalance (Modal)/Destination Input',
  swapDirectionButton = 'Rebalance (Modal)/Swap Direction Button',
  reviewButton = 'Rebalance (Modal)/Review Button'
}
