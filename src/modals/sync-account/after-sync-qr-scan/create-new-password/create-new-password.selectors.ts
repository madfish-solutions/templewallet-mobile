export enum CreateNewPasswordSyncAccountSelectors {
  passwordInput = 'Create a new password (Sync)/Password Input',
  repeatPasswordInput = 'Create a new password (Sync)/Repeat Password Input',
  syncButton = 'Create a new password (Sync)/Sync Button'
}
