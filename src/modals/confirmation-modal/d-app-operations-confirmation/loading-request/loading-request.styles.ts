import { createUseStyles } from 'src/styles/create-use-styles';

export const useLoadingRequestStyles = createUseStyles(() => ({
  container: {
    flexGrow: 1,
    justifyContent: 'space-between'
  }
}));
