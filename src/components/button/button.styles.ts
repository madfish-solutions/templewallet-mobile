import { StyleSheet } from 'react-native';

export const ButtonStyles = StyleSheet.create({
  containerStyle: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center'
  }
});
