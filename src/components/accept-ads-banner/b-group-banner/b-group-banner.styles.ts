import { createUseStyles } from 'src/styles/create-use-styles';

export const useBGropuBannerStyles = createUseStyles(() => ({
  buttons: {
    flexDirection: 'row',
    justifyContent: 'space-between'
  },
  buttonContainer: {
    flex: 1
  }
}));
