import { StyleSheet } from 'react-native';

import { formatSize } from '../../../styles/format-size';

export const ModalHeaderCloseIconStyles = StyleSheet.create({
  icon: {
    marginHorizontal: formatSize(16)
  }
});
